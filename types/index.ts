export type WindowStyle = "mac" | "windows" | "none";

export interface Background {
  id: string;
  label: string;
  value: string;
  swatch: string;
}

export interface Language {
  id: string;
  label: string;
}